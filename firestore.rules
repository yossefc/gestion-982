rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper: vérifier si l'utilisateur est authentifié
    function isAuthenticated() {
      return request.auth != null;
    }

    // Helper: vérifier le rôle (via custom claims)
    function hasRole(role) {
      return isAuthenticated() && request.auth.token.role == role;
    }

    // ==========================================
    // RÈGLES TEMPORAIRES POUR DÉVELOPPEMENT
    // ==========================================
    // TODO: Remplacer par les règles RBAC ci-dessous en production

    match /{document=**} {
      allow read, write: if isAuthenticated();
    }

    // ==========================================
    // RÈGLES RBAC (À ACTIVER EN PRODUCTION)
    // ==========================================
    // Décommenter ces règles et commenter la règle temporaire ci-dessus

    /*
    // Collection: users
    match /users/{userId} {
      // Les admins peuvent lire/modifier tous les utilisateurs
      allow read: if hasRole('admin');
      allow write: if hasRole('admin');

      // Chaque utilisateur peut lire ses propres données
      allow read: if isAuthenticated() && request.auth.uid == userId;
    }

    // Collection: soldiers
    match /soldiers/{soldierId} {
      // Tous les utilisateurs authentifiés peuvent lire
      allow read: if isAuthenticated();

      // Seuls admin et both peuvent créer/modifier
      allow create, update: if hasRole('admin') || hasRole('both') || hasRole('arme') || hasRole('vetement');

      // Seuls les admins peuvent supprimer
      allow delete: if hasRole('admin');
    }

    // Collection: equipment_combat
    match /equipment_combat/{equipmentId} {
      // Lecture: admin, both, arme
      allow read: if hasRole('admin') || hasRole('both') || hasRole('arme');

      // Écriture: admin, both, arme
      allow write: if hasRole('admin') || hasRole('both') || hasRole('arme');
    }

    // Collection: equipment_clothing
    match /equipment_clothing/{equipmentId} {
      // Lecture: admin, both, vetement
      allow read: if hasRole('admin') || hasRole('both') || hasRole('vetement');

      // Écriture: admin, both, vetement
      allow write: if hasRole('admin') || hasRole('both') || hasRole('vetement');
    }

    // Collection: manot
    match /manot/{manaId} {
      // Lecture: admin, both, arme
      allow read: if hasRole('admin') || hasRole('both') || hasRole('arme');

      // Écriture: admin, both, arme
      allow write: if hasRole('admin') || hasRole('both') || hasRole('arme');
    }

    // Collection: assignments
    match /assignments/{assignmentId} {
      // Tous les utilisateurs authentifiés peuvent lire les attributions
      allow read: if isAuthenticated();

      // Création: admin, both, arme (pour combat), vetement (pour clothing)
      allow create: if isAuthenticated();

      // Mise à jour: admin, both, ou l'utilisateur qui a créé
      allow update: if hasRole('admin') || hasRole('both') ||
                       resource.data.assignedBy == request.auth.uid;

      // Suppression: admin uniquement
      allow delete: if hasRole('admin');
    }
    */
  }
}
